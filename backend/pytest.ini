[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: requires a running MT5 terminal and demo credentials
    safety_integration: offline integration tests for the safety layer; never sends MT5 orders
filterwarnings =
    ignore::starlette.exceptions.StarletteDeprecationWarning
